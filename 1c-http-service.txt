Функция tv_nku_sklad(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);  
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
				"ВЫБРАТЬ ПЕРВЫЕ 10
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.Номер,
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.Дата,
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.НаименованиеНКУ,
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.Получатель.Физлицо.Наименование КАК Получатель,
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.ЗаказПокупателя.Номер КАК ЗаказНомер,
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.ЗаказПокупателя.Дата КАК ЗаказДата,
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.ЗаказПокупателя.Контрагент.Наименование КАК ЗаказКонтрагент
				|ИЗ
				|	Документ.ЗаявкаНаПолучениеОборудованияДляНКУ КАК ЗаявкаНаПолучениеОборудованияДляНКУ
				|ГДЕ
				|	ЗаявкаНаПолучениеОборудованияДляНКУ.Выдано = ЛОЖЬ";
				
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();

	МассивЗаявок = Новый Массив;
	Пока Выборка.Следующий() Цикл    
		Заявка = Новый Структура;
		Заявка.Вставить("Date", Выборка.Дата);
		Заявка.Вставить("Num", Выборка.Номер);
		Заявка.Вставить("ZakazDate", Выборка.ЗаказДата);
		Заявка.Вставить("ZakazNum", Выборка.ЗаказНомер);
		Заявка.Вставить("ZakazKontragent", Выборка.ЗаказКонтрагент);
		Заявка.Вставить("NKU", Выборка.НаименованиеНКУ);
		Заявка.Вставить("Poluchatel", Выборка.Получатель);
		МассивЗаявок.Добавить(Заявка);
	КонецЦикла;                                           
	
	Запись = Новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись, МассивЗаявок, Новый НастройкиСериализацииJSON);
	СтрокаОтвета = Запись.Закрыть();
	
	Ответ.УстановитьТелоИзСтроки(СтрокаОтвета, КодировкаТекста.UTF8);	
	Ответ.Заголовки.Вставить("Content-Type","application/json; charset=utf-8");     
	Возврат Ответ;
КонецФункции

